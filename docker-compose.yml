version: '3.3'

services:
   mysql:
     image: mysql:5.7
     restart: always
     env_file:
       - .env
   wordpress:
     depends_on:
       - mysql
     image: wordpress:latest
     ports:
       - "8000:80"
     restart: always
     env_file:
       - .env
